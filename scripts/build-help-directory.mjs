import {readFileSync,writeFileSync} from 'node:fs';
const root=new URL('../',import.meta.url);
const data=JSON.parse(readFileSync(new URL('data/help-directory.json',root),'utf8'));
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const categories=Object.entries(data.categories).sort((a,b)=>a[1].localeCompare(b[1],'es'));
let html=readFileSync(new URL('webs-amigas.html',root),'utf8');
const nav='<nav class="friends-nav" aria-label="Ir a una necesidad">'+categories.map(([id,name])=>`<a href="#${id}">${esc(name)}</a>`).join('')+'</nav>';
const sections=categories.map(([id,name])=>`<section class="friends-group" id="${id}" aria-labelledby="title-${id}"><h2 id="title-${id}">${esc(name)}</h2><div class="friends-grid">${data.records.filter(r=>r.category===id).sort((a,b)=>a.name.localeCompare(b.name,'es')).map(r=>`<a class="friends-card" data-organization="${r.id}" href="${esc(r.url)}" rel="noreferrer"><h3 lang="${r.language}">${esc(r.name)}</h3><p>${esc(r.description)}</p><small>${esc(data.countries[r.country])} · ${r.language.toUpperCase()} · <time datetime="${r.reviewedAt}">${r.reviewedAt}</time></small><span class="friends-link">${esc(new URL(r.url).hostname.replace(/^www\./,''))} →</span></a>`).join('\n')}</div></section>`).join('\n');
html=html.replace(/<nav class="friends-nav"[\s\S]*?(?=<section class="friends-method")/,nav+sections+'\n')
 .replace(/<title>[^<]*<\/title>/,'<title>Webs amigas: 200 recursos por país y necesidad | Desgracias.es</title>')
 .replace(/(<meta name="description" content=")[^"]+/, '$1Directorio independiente con 200 recursos y organizaciones: España, México, Latinoamérica, Reino Unido, Francia, Portugal y Unión Europea.')
 .replace(/(<meta property="og:title" content=")[^"]+/, '$1Webs amigas: 200 recursos por país y necesidad')
 .replace(/<p class="eyebrow">[^<]*<\/p>/,'<p class="eyebrow">Directorio independiente · Recursos por país</p>')
 .replace(/<p class="friends-lead">[\s\S]*?<\/p>/,`<p class="friends-lead">${data.records.length} recursos y organizaciones, ordenados alfabéticamente por categoría y nombre. Incluye varios servicios de una misma institución: no son 200 entidades distintas. Su presencia no implica colaboración, patrocinio ni acuerdo con Desgracias.es. <a href="/ayuda/es/">Elegir país y necesidad</a> · <a href="/ayuda/en/" lang="en">Help in English</a></p>`)
 .replace(/<section class="friends-method">[\s\S]*?<\/section>/,'<section class="friends-method"><h2>Cómo seleccionamos estas webs</h2><p>Fuentes oficiales y organizaciones identificables. Cada ficha indica país, idioma de destino y fecha de revisión. Las 150 incorporaciones del 20 de septiembre de 2026 se comprobaron por respuesta y título; las 50 entidades anteriores conservan su revisión del 18 de septiembre, salvo ANAR, revisada el día 20. No certificamos la calidad de la atención ni garantizamos disponibilidad.</p><p>En los recursos de la UE, consulta las condiciones del país. En Reino Unido, la cobertura puede variar entre Inglaterra, Escocia, Gales e Irlanda del Norte. Algunas entidades españolas tienen cobertura local o autonómica. Comprueba los requisitos, horarios y privacidad antes de compartir datos. No transmitimos tu consulta a estas entidades.</p><p><a href="/como-revisamos.html">Cómo revisamos</a> · <a href="/contacto.html">Comunicar una corrección</a> · <a href="/privacidad.html">Privacidad</a></p></section>');
html=html.replace(/"name":"Webs amigas: organizaciones de ayuda en España"/,'"name":"Webs amigas: recursos por país"').replace('"dateModified":"2026-09-18"','"dateModified":"2026-09-20"').replace('Directorio editorial independiente de organizaciones de ámbito nacional con fuentes oficiales.','Directorio independiente de recursos y organizaciones por país con fuentes oficiales.');
writeFileSync(new URL('webs-amigas.html',root),html);
