name: One-off Postvention Resource Repair

on:
  push:
    branches: [safety/repair-postvention-live-resources]
    paths:
      - 'scripts/one-off-repair-postvention-resources.mjs'
      - '.github/workflows/one-off-postvention-repair.yml'

permissions:
  contents: write

jobs:
  repair:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repair branch
        uses: actions/checkout@v7
        with:
          ref: safety/repair-postvention-live-resources

      - name: Use Node.js 24
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          package-manager-cache: false

      - name: Apply fail-closed resource replacements
        run: node scripts/one-off-repair-postvention-resources.mjs

      - name: Verify focused regression test
        run: node --test tests/suicide-bereavement-live-resources.test.mjs

      - name: Commit generated repair
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add duelo/ha-muerto-por-suicidio-alguien-que-quiero/index.html sitemap.xml
          git diff --cached --exit-code && { echo 'Expected generated repair diff is empty'; exit 1; } || true
          git commit -m 'safety: repair live suicide-postvention resources'
          git push origin HEAD:safety/repair-postvention-live-resources
